# Source Directory (The "Zurg" mount or source media folder)
# In Docker, this maps to /app/source inside the container
SOURCE_PATH=/mnt/zurg

# Destination Directory (The "16tb" drive or destination storage)
# In Docker, this maps to /app/destination inside the container
DESTINATION_PATH=/mnt/16tb

# Database persist directory (optional override)
# DATA_DIR=./data

# Timezone
TZ=UTC

# Security
# Generate a secure key using: openssl rand -base64 32
JWT_SECRET_KEY=change_this_to_a_secure_random_string
